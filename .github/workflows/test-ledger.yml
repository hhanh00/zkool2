name: Test Ledger

on:
  workflow_dispatch:

jobs:
  emulator:
    runs-on: ubuntu-latest
    env:
      SEED: ${{ secrets.SEED }}
    steps:
      - name: Install RUST
        uses: dtolnay/rust-toolchain@stable
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config protobuf-compiler
      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust
      - name: Setup emulator
        run: |
          misc/ledger/setup.sh
          misc/ledger/build.sh
          misc/ledger/run-emulator.sh
      - name: Verify emulator
        run: |
          response=$(curl -s -m 5 -X POST http://127.0.0.1:9999 \
            -H 'Content-Type: application/json' -d '{"apduHex": "e0c4000000"}')
          echo "$response"
          echo "$response" | grep -qE '"data": "3830[0-9a-f]*9000", "error": null'
      # Each test returns only once the app is back on its home screen, so
      # the emulator needs no restart between them.
      - name: Run ledger tests
        run: |
          cd rust
          cargo test --features zemu -- --ignored --nocapture ledger_app_version
          cargo test --features zemu -- --ignored --nocapture ledger_official_sign
          cargo test --features zemu -- --ignored --nocapture ledger_get_ufvk
          cargo test --features zemu -- --ignored --nocapture ledger_account_import
