name: Release Please
permissions:
  contents: write
  pull-requests: write

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      major: ${{ steps.release.outputs.major }}
      minor: ${{ steps.release.outputs.minor }}
    steps:
      - uses: actions/checkout@v6
      - id: release
        uses: googleapis/release-please-action@v5
        with:
          token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
      - if: steps.release.outputs.pr
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
        run: |
          BRANCH="release-please--branches--main--components--zkool"
          git fetch origin "$BRANCH"
          git checkout "$BRANCH"
          git config user.name "$(git log -1 --format='%an')"
          git config user.email "$(git log -1 --format='%ae')"
          BUILD=$(cat build_number.txt)
          NEW_BUILD=$((BUILD + 1))
          echo "$NEW_BUILD" > build_number.txt
          git add build_number.txt
          git commit --amend --no-edit
          git push --force "https://x-access-token:$(gh auth token)@github.com/$GITHUB_REPOSITORY" "$BRANCH"
